Show that game!
<% game = @game %>
<%= game.id %>
<br><br>
<% game.planets.each do |planet| %>
  <%= link_to planet.name, game_planet_path(game, planet) %>
  <br>
<% end %>

<%= react_component 'Game', { data: @game } %>